### __Seed builder__
#   Autogenerated jobs
#   Add custom jobs at the end

name: django_reference_test

on:
  push:
    branches: [dev, pre-merge]

  pull_request:
    branches:
      - '**'

jobs:

  test:
    uses: ./.github/workflows/test_wf.yml

# __End autogenerated__
# Include jobs after this block
###