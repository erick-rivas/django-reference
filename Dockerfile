### __Seed builder__
#   Autogenerated commands
#   Add custom commands at the end

FROM python:3.11.4
WORKDIR /app
ENV PYTHONDONTWRITEBYTECODE 1
ENV PYTHONUNBUFFERED=1
RUN pip install --upgrade pip==23.2.1
RUN apt update
RUN apt install -y zlib1g-dev build-essential libssl-dev libreadline-dev libyaml-dev libxml2-dev libxslt1-dev libcurl4-openssl-dev software-properties-common libffi-dev yarn postgresql-client
ENV TZ=America/Mexico_City

# __End autogenerated__
# Include commands after this block
###

COPY requirements.txt .
COPY seed/requirements.txt seed/
RUN pip install -r requirements.txt --no-cache-dir
COPY . .